import { mapGetters, mapActions, mapState } from 'vuex'

export default {
  computed: {
    ...mapState({
    }),
    ...mapGetters({
    })
  },
  data () {
    return {}
  },
  async created () {
  },
  methods: {
    ...mapActions({
    })
  },
  mounted () {
  },
  beforeDestroy () {
  }
}
